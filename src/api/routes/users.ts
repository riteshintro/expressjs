import { Router } from 'express';
// import { getUsers, createUser } from '../controllers/users/users';

const router = Router();

// router.get('/', getUsers);
// router.post('/create', createUser);

export default router;